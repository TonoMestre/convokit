# ConvoKit backend — SQLite persistence layer.
#
# All SQLite logic lives here: schema creation and CRUD for convocatorias and
# their generated deliverables. The schema is designed to map cleanly onto a
# future Supabase migration (same relational model).
#
# Implemented in Step 2 of the implementation order (see CLAUDE.md).
