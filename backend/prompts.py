# ConvoKit backend — Claude system prompts.
#
# One system prompt per deliverable type (outputs 1-7), stored in a dictionary
# indexed by output number. Prompts must never be embedded in the endpoints.
#
# Implemented across Steps 5 and 8 of the implementation order (see CLAUDE.md).
