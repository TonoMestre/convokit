# ConvoKit backend — structured JSON exporters.
#
# Converts outputs 6 (convocatoria profile) and 7 (document tree) into the
# structured JSON schema defined in section 12 of the PRD. This JSON feeds the
# second application (App de Memorias); the field schema must not change without
# coordinating with that app's data model.
#
# Implemented in Step 8 bis of the implementation order (see CLAUDE.md).
